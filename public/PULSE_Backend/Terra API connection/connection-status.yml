info:
  name: connection-status
  type: http
  seq: 3
  tags:
    - Terra API connection

http:
  method: GET
  url: "{{baseUrl}}/api/v1/terra/connections"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/terra/connections"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

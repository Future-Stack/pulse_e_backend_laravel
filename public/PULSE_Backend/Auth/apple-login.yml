info:
  name: apple-login
  type: http
  seq: 11
  tags:
    - Auth

http:
  method: POST
  url: "{{baseUrl}}/api/v1/apple"
  body:
    type: json
    data: |-
      {
        "apple_token": "dummy_apple_token_9876543210_test_value",
        "full_name": "Rehan Kabir Mim",
        "email": "rehan@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/apple"
      method: POST
      body:
        type: json
        data: |-
          {
            "apple_token": "dummy_apple_token_9876543210_test_value",
            "full_name": "Rehan Kabir Mim",
            "email": "rehan@example.com"
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

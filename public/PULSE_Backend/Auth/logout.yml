info:
  name: logout
  type: http
  seq: 8
  tags:
    - Auth

http:
  method: POST
  url: "{{base_url}}/logout"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/logout"
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

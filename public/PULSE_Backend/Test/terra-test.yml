info:
  name: terra-test
  type: http
  seq: 1
  tags:
    - Test

http:
  method: POST
  url: "{{baseUrl}}/terra/widget-session"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/terra/widget-session"
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

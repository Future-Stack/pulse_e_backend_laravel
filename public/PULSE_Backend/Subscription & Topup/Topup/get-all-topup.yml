info:
  name: get-all-topup
  type: http
  seq: 2
  tags:
    - Subscription & Topup/Topup

http:
  method: GET
  url: "{{baseUrl}}/topups"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/topups"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

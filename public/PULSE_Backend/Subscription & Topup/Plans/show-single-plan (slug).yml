info:
  name: show-single-plan (slug)
  type: http
  seq: 2
  tags:
    - Subscription & Topup/Plans

http:
  method: GET
  url: "{{baseUrl}}/subscription-plan/premium"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/subscription-plan/premium"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

info:
  name: show-all-plan
  type: http
  seq: 1
  tags:
    - Subscription & Topup/Plans

http:
  method: GET
  url: "{{baseUrl}}/subscription-plans"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/subscription-plans"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

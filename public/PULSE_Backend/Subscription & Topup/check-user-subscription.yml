info:
  name: check-user-subscription
  type: http
  seq: 4

http:
  method: GET
  url: "{{url_local}}/check-user-subscription"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

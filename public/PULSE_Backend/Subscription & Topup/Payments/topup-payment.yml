info:
  name: topup-payment
  type: http
  seq: 2
  tags:
    - Subscription & Topup/Payments

http:
  method: POST
  url: "{{base_url}}/topup-payment"
  body:
    type: json
    data: |-
      {
        "slug": "coaching_sessions_20" //skin_scans_5, coaching_sessions_20
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

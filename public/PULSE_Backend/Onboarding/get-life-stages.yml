info:
  name: get-life-stages
  type: http
  seq: 1
  tags:
    - Onboarding

http:
  method: GET
  url: "{{baseUrl}}/api/v1/life-stages"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/life-stages"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

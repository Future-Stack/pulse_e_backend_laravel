info:
  name: get-life-journeys
  type: http
  seq: 4
  tags:
    - Onboarding

http:
  method: GET
  url: "{{baseUrl}}/life-journeys"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/life-journeys"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

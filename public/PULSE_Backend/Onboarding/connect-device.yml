info:
  name: connect-device
  type: http
  seq: 5
  tags:
    - Onboarding

http:
  method: GET
  url: "{{baseUrl}}/api/v1/connect-devices"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/connect-devices"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

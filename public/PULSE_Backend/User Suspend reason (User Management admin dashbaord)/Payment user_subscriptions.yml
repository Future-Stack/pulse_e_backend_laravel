info:
  name: Payment user_subscriptions
  type: http
  seq: 5
  tags:
    - User Suspend reason (User Management admin dashbaord)

http:
  method: GET
  url: "{{baseUrl}}/api/v1/user/subscription"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/user/subscription"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

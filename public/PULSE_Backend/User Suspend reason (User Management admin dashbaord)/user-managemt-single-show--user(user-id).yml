info:
  name: user-managemt-single-show--user(user-id)
  type: http
  seq: 4
  tags:
    - User Suspend reason (User Management admin dashbaord)

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users-details/2"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/users-details/2"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

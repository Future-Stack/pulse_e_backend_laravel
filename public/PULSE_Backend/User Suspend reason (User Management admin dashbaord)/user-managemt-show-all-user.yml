info:
  name: user-managemt-show-all-user
  type: http
  seq: 3
  tags:
    - User Suspend reason (User Management admin dashbaord)

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/users"
      method: GET
      body:
        type: json
        data: ""
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

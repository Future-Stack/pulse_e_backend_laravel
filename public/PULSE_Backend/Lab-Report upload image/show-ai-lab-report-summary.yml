info:
  name: show-ai-lab-report-summary
  type: http
  seq: 3
  tags:
    - Lab-Report upload image

http:
  method: GET
  url: "{{baseUrl}}/api/v1/ai-lab-reports/3"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/ai-lab-reports/3"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

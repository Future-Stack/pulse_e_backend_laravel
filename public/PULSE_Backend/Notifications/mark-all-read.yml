info:
  name: mark-all-read
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/notifications/read-all"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: fetch-admin-notifications (read)
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/admin-read-notifications"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

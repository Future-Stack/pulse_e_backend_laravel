info:
  name: fetch-admin-notifications (all)
  type: http
  seq: 1
  tags:
    - Notifications

http:
  method: GET
  url: "{{base_url}}/admins-notifications"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

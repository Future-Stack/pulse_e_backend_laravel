info:
  name: snapshort-homepage(using user id)
  type: http
  seq: 2
  tags:
    - AI Part

http:
  method: GET
  url: "{{baseUrl}}/api/v1/snapshot/3"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/snapshot/3"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

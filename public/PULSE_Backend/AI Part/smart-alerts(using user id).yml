info:
  name: smart-alerts(using user id)
  type: http
  seq: 3
  tags:
    - AI Part

http:
  method: GET
  url: "{{baseUrl}}/api/v1/smart-analysis/4"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/smart-analysis/4"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

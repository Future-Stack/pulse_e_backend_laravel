info:
  name: Chatbot
  type: http
  seq: 6
  tags:
    - AI Part

http:
  method: POST
  url: "{{baseUrl}}/api/v1/chat/response"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/chat/response"
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

info:
  name: waitlist-entry
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/waitlist/submit"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "full_name": "Sabbir Hossain",
        "email": "bsabbir76@gmail.com"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: decline-post (for admin)
  type: http
  seq: 12
  tags:
    - Community Post

http:
  method: POST
  url: "{{baseUrl}}/api/v1/posts/1/decline"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/posts/1/decline"
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

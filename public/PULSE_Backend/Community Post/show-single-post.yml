info:
  name: show-single-post
  type: http
  seq: 3
  tags:
    - Community Post

http:
  method: GET
  url: "{{baseUrl}}/api/v1/posts/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/posts/1"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

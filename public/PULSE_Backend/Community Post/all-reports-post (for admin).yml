info:
  name: all-reports-post (for admin)
  type: http
  seq: 10
  tags:
    - Community Post

http:
  method: GET
  url: "{{baseUrl}}/api/v1/reports"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/reports"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

info:
  name: delete-post (for admin & post owner)
  type: http
  seq: 5
  tags:
    - Community Post

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/posts/3"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/posts/3"
      method: DELETE
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

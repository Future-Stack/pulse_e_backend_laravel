info:
  name: show-single(page_id) without auth
  type: http
  seq: 2
  tags:
    - Pages

http:
  method: GET
  url: "{{baseUrl}}/pages/1"
  params:
    - name: url
      value: http://127.0.0.1:8000/api/v1
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/pages/1"
      method: GET
      params:
        - name: url
          value: http://127.0.0.1:8000/api/v1
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
